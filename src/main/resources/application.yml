spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mydb?useSSL=false
    username: root
    password: 

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming:
        strategy: org.hibernate.cfg.ImprovedNomingStrategy
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
  
  thymeleaf:
    cache: false
    
banner:
  location: classpath:/example/banner.txt

  